@model IEnumerable<NongSanDungHa.Models.ADO.product_review>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}

<h2>Đánh giá người dùng</h2>
<form class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search" action="/Admin/product_review/SearchResult" method="get">
    <div class="input-group">
        <input type="search" class="form-control bg-light border-0 small" placeholder="Search by user id"
               aria-label="Search" aria-describedby="basic-addon2" name="searchKey">
        <div class="input-group-append">
            <button class="btn btn-primary" style="background-color: #52a851;" type="submit" name="submit">
                <i class="fas fa-search fa-sm"></i>
            </button>
        </div>
    </div>
</form>
<p>
    @Html.ActionLink("Thêm mới", "CreateNew")
</p>
<p>

    Lượt review: @ViewBag.TotalProductReview
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.user_account_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product_review_content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.review_owner)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.user_account_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.product_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.product_review_content)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.review_owner)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.product_review_id }) |
                @Html.ActionLink("Chi tiết", "Detail", new { id = item.product_review_id }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.product_review_id })
            </td>
        </tr>
    }

</table>
