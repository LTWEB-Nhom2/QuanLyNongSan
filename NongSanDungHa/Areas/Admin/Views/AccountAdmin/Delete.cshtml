@model NongSanDungHa.Models.ADO.admin_account

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>admin_account</h4>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.admin_username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.admin_username)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.admin_password)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.admin_password)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model => model.admin_account_id)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
    @{
        if (ViewBag.KT == 0)
        {
            <div class="alert alert-danger" role="alert">
                @ViewBag.Message
            </div>
        }
    }
</div>
